name: Legacy TorrServer updater (disabled)

on:
  workflow_dispatch:

# The active updater is .github/workflows/upstream-update.yml.
# This legacy workflow is intentionally kept without a schedule so there is
# only one automated upstream update mechanism in the project.

permissions: {}

jobs:
  disabled:
    runs-on: ubuntu-latest
    steps:
      - run: echo "Legacy updater disabled. Use upstream-update.yml."
